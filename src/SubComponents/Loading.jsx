import React from "react";

const Loading = ({}) => {
  return (
    <div className="flex justify-center items-center h-screen">
      <span className="loading loading-ring loading-lg text-orange"></span>
    </div>
  );
};

export default Loading;
